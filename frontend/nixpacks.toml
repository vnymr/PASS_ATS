# Nixpacks configuration for frontend
[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[variables]
NODE_ENV = "production"
VITE_API_URL = "/api"

[start]
cmd = "npx serve -s dist -l 8080 --single --no-clipboard"
