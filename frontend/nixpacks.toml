[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "rm -f .env .env.local .env.production .env.staging",
  "echo 'VITE_CLERK_PUBLISHABLE_KEY='$VITE_CLERK_PUBLISHABLE_KEY",
  "npm run build"
]

[start]
cmd = "npm start"